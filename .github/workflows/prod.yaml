name: Publish to Pre Prod

on:
  release:
    types: 
      - released
      
jobs:
  validate-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Check tag name
        run: |
          TAG_NAME="${{github.ref_name}}"
          if [[ ! "$TAG_NAME" =~ ^[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3} ]]; then
            echo "❌ Invalid tag name: $TAG_NAME"
            exit 1
          else
            echo "✅ Valid tag: $TAG_NAME"
          fi
          
          echo ${{github.ref_name}}